<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Acquired Items - E-Waste Management</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            color: #333;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 20px;
        }

        .header {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 30px;
            margin-bottom: 30px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .header h1 {
            color: #4a7c59;
            font-size: 2.5rem;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 15px;
        }

        .header p {
            color: #666;
            font-size: 1.1rem;
        }

        .controls {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 30px;
            flex-wrap: wrap;
            gap: 15px;
        }

        .back-btn {
            background: linear-gradient(135deg, #4a7c59 0%, #6fa774 100%);
            color: white;
            padding: 12px 24px;
            border: none;
            border-radius: 50px;
            font-size: 1rem;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .back-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(74, 124, 89, 0.3);
        }

        .export-btn {
            background: linear-gradient(135deg, #28a745 0%, #20c997 100%);
            color: white;
            padding: 12px 24px;
            border: none;
            border-radius: 50px;
            font-size: 1rem;
            cursor: pointer;
            transition: all 0.3s ease;
            text-decoration: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .export-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(40, 167, 69, 0.3);
        }

        .items-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(350px, 1fr));
            gap: 25px;
            margin-top: 30px;
        }

        .item-card {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            padding: 25px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            transition: all 0.3s ease;
            border-left: 5px solid #4a7c59;
        }

        .item-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
        }

        .item-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }

        .item-id {
            font-weight: 600;
            color: #4a7c59;
            font-size: 1.1rem;
        }

        .item-type {
            background: linear-gradient(135deg, #4a7c59 0%, #6fa774 100%);
            color: white;
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 0.9rem;
            font-weight: 500;
        }

        .item-details {
            margin-bottom: 15px;
        }

        .detail-row {
            display: flex;
            justify-content: space-between;
            margin-bottom: 8px;
            padding: 8px 0;
            border-bottom: 1px solid #eee;
        }

        .detail-row:last-child {
            border-bottom: none;
        }

        .detail-label {
            font-weight: 600;
            color: #555;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .detail-value {
            color: #333;
            text-align: right;
        }

        .hazard-high {
            color: #dc3545;
            font-weight: 600;
        }

        .hazard-medium {
            color: #ffc107;
            font-weight: 600;
        }

        .hazard-low {
            color: #28a745;
            font-weight: 600;
        }

        .data-sensitive {
            color: #dc3545;
            font-weight: 600;
        }

        .data-normal {
            color: #28a745;
            font-weight: 600;
        }

        .no-items {
            text-align: center;
            padding: 60px 20px;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        }

        .no-items i {
            font-size: 4rem;
            color: #ccc;
            margin-bottom: 20px;
        }

        .no-items h3 {
            color: #666;
            margin-bottom: 10px;
            font-size: 1.5rem;
        }

        .no-items p {
            color: #999;
            font-size: 1.1rem;
        }

        @media (max-width: 768px) {
            .container {
                padding: 15px;
            }

            .header h1 {
                font-size: 2rem;
                flex-direction: column;
                gap: 10px;
            }

            .controls {
                flex-direction: column;
                align-items: stretch;
            }

            .items-grid {
                grid-template-columns: 1fr;
            }

            .item-card {
                padding: 20px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>
                <i class="fas fa-shopping-cart"></i>
                Acquired Items
            </h1>
            <p>Items you have successfully acquired through bidding</p>
        </div>

        <div class="controls">
            <a href="/recycler/dashboard" class="back-btn">
                <i class="fas fa-arrow-left"></i>
                Back to Dashboard
            </a>
            <% if (items && items.length > 0) { %>
            <button onclick="exportToCSV()" class="export-btn">
                <i class="fas fa-download"></i>
                Export CSV
            </button>
            <% } %>
        </div>

        <% if (items && items.length > 0) { %>
        <div class="items-grid">
            <% items.forEach(item => { %>
            <div class="item-card">
                <div class="item-header">
                    <div class="item-id">
                        <i class="fas fa-tag"></i>
                        <%= item.unique_id %>
                    </div>
                    <div class="item-type">
                        <%= item.type %>
                    </div>
                </div>

                <div class="item-details">
                    <div class="detail-row">
                        <span class="detail-label">
                            <i class="fas fa-barcode"></i>
                            Serial No:
                        </span>
                        <span class="detail-value"><%= item.serial_no || 'N/A' %></span>
                    </div>

                    <div class="detail-row">
                        <span class="detail-label">
                            <i class="fas fa-layer-group"></i>
                            Category:
                        </span>
                        <span class="detail-value"><%= item.category || 'N/A' %></span>
                    </div>

                    <div class="detail-row">
                        <span class="detail-label">
                            <i class="fas fa-exclamation-triangle"></i>
                            Hazard Level:
                        </span>
                        <span class="detail-value hazard-<%= (item.hazard || 'low').toLowerCase() %>">
                            <%= item.hazard || 'Low' %>
                        </span>
                    </div>

                    <div class="detail-row">
                        <span class="detail-label">
                            <i class="fas fa-shield-alt"></i>
                            Data Sensitivity:
                        </span>
                        <span class="detail-value data-<%= item.data_sensitivity === 'Sensitive' ? 'sensitive' : 'normal' %>">
                            <%= item.data_sensitivity || 'Normal' %>
                        </span>
                    </div>

                    <div class="detail-row">
                        <span class="detail-label">
                            <i class="fas fa-calendar-alt"></i>
                            Decommissioned:
                        </span>
                        <span class="detail-value">
                            <%= item.date_decommissioned ? new Date(item.date_decommissioned).toLocaleDateString() : 'N/A' %>
                        </span>
                    </div>
                </div>
            </div>
            <% }); %>
        </div>
        <% } else { %>
        <div class="no-items">
            <i class="fas fa-inbox"></i>
            <h3>No Acquired Items</h3>
            <p>You haven't acquired any e-waste items yet. Start bidding to acquire items!</p>
        </div>
        <% } %>
    </div>

    <script>
        function exportToCSV() {
            // Show loading state
            const exportBtn = document.querySelector('.export-btn');
            const originalText = exportBtn.innerHTML;
            exportBtn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Generating CSV...';
            exportBtn.disabled = true;

            // Create a temporary link to download the CSV
            const link = document.createElement('a');
            link.href = '/recycler/acquired-items/export-csv';
            link.download = ''; // Let the server set the filename
            document.body.appendChild(link);
            link.click();
            document.body.removeChild(link);

            // Reset button state after a short delay
            setTimeout(() => {
                exportBtn.innerHTML = originalText;
                exportBtn.disabled = false;
            }, 2000);
        }
    </script>
</body>
</html>